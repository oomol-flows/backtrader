version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
icon: ./icon.png
name: backtrader
repository: https://github.com/oomol-flows/backtrader
releaseNotes: init back-trader flow
displayName: Backtrader
description: Using the backtrader library to assist investors with stock
  analysis and backtesting
hide_source: true
